# Copy this file to .env and adjust values as needed.
# No secrets are required for local development by default.

# Optional: override the default log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
